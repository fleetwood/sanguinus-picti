exports.seed = function(knex, Promise) {
  // const schema = knex.schema;
  // // Deletes ALL existing entries
  // try {
  //   schema
  //     .hasTable('users')
  //     .then((hasUsers) => {
  //       if (hasUsers) {
  //         return knex.schema.raw(`TRUNCATE TABLE users RESTART IDENTITY CASCADE;`)
  //       }
  //     })
  //     .then(() => schema.hasTable('pages'))
  //     .then((hasPages) => {
  //       if (hasPages) {
  //         return knex.schema.raw(`TRUNCATE TABLE pages RESTART IDENTITY CASCADE;`);
  //       }
  //     });
  // } catch(e) {}
};
